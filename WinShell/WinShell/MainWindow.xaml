<Window x:Class="WinShell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinShell"
        mc:Ignorable="d"
        Title="WinShell" Height="350" Width="525"
        >
    <Border BorderBrush="LightGray" BorderThickness="5px">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" FontFamily="Consolas" Foreground="#FF1E0BD8">This is my text.</TextBlock>
            <TextBlock Grid.Row="1" FontFamily="Consolas" Text="{Binding CurrentWorkingDirectory}" TextWrapping="Wrap"/>
            <TextBox Grid.Row="2" FontFamily="Consolas" TextWrapping="Wrap">(Enter a command here!)</TextBox>
        </Grid>
    </Border>
</Window>
